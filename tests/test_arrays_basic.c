int
main()
{
    // Basic array declaration and initialization
    int arr[5] = {1, 2, 3, 4, 5};

    // Accessing elements
    int a = arr[0];
    int b = arr[2];
    int c = arr[4];

    // Modifying an element
    arr[1] = 20;
    int x = arr[1];

    return x - 20;
}
